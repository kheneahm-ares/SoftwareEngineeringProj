@{
    ViewData["Title"] = "Create";
}

<div class="container">
    <div class="col-md-4">
        <label> Select Form Type</label>
        <select id="myselect" required>
            @foreach (var category in ViewBag.Categories)
            {

                if (category.CategoryId == '1')
                {

                    <option value="@category.CategoryId" selected="selected">@category.Name</option>

                }
                else
                {
                    <option value="@category.CategoryId">@category.Name</option>
                }


            }
        </select>

    </div>

    <div class="col-md-8 col-md-offset-4" style="position:relative; top: -50px;">
        @{ await Html.RenderPartialAsync("MultipleChoice");}

        <form asp-action="Create" id="2">asdfasdf</form>

        <form asp-action="Create" id="3">okjihkjbhkhijopiohkbhkjioikbh</form>

    </div>
</div>
@*<form asp-action="Create">
    <div class="form-horizontal">
        <h4>Post</h4>
        <hr />
        <div asp-validation-summary="ModelOnly" class="text-danger"></div>
        <div class="form-group">
            <label asp-for="Categories" class="col-md-2 control-label"></label>
            <div class="col-md-10">
                <select name="category" asp-for="Categories" size="1">
                    @foreach(var category in Model.Categories)
                    {
                        <option value="@category.CategoryId">@category.Name</option>

                    }
                </select>
            </div>
        </div>
        <div class="form-group">
            <div class="col-md-offset-2 col-md-10">
                <input type="submit" value="Create" class="btn btn-default" />
            </div>
        </div>
    </div>
</form>*@

@*<div>
    <a asp-action="Index">Back to List</a>
</div>*@



<script>
    $(document).ready(function () {
        $('#2').hide();
        $('#3').hide();
        $('#1').show("slow");
        $("#myselect").change(function () {
            var value = $(this).val();
            $('#1').hide();
            $('#2').hide();
            $('#3').hide();
            $('#' + value).show("slow");
            
        });
      
    });
</script>
